# Public shell boundary

This private workspace package will own shared static-site mechanics such as metadata, skip links, canonical helpers, and legal footer structure.

It must not contain auth logic, tenant data, product-specific presentation, or a competing design system.
