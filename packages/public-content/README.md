# Public content boundary

This private workspace package will own typed, public-safe content and route metadata as pages move from the legacy renderer.

It must not contain auth logic, tenant data, private route types, or a competing design system.
